apiVersion: v1
kind: ResourceQuota
metadata:
  name: platform-quota
  namespace: {{ .Release.Namespace }}
spec:
  hard:
    requests.cpu: "10"
    requests.memory: 20Gi
    limits.cpu: "20"
    limits.memory: 40Gi
    persistentvolumeclaims: "10"
    pods: "50"
    services: "20"
    secrets: "50"
    configmaps: "50"
